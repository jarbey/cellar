{% extends 'base.html.twig' %}

{% block body %}
	<table>
		{% for sensor in db.sensors %}
			<tr>
				<td><img src="/api/{{ db.id }}/sensors/{{ sensor.id }}/graph/temperature?t={{ date|date('U') }}" /></td>
				<td><img src="/api/{{ db.id }}/sensors/{{ sensor.id }}/graph/humidity?t={{ date|date('U') }}" /></td>
			</tr>
		{% endfor %}
	</table>
{% endblock %}